# Evaluation

No trained checkpoint is bundled. Run `python train.py` after placing normalized splits in `data/processed/`, then export the best checkpoint to `models/fallacy_classifier.onnx` with Optimum. The report generated by training includes accuracy, precision, recall, F1, and a confusion matrix on the held-out test split.
